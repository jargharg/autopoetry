import React, { Component } from "react"

const PoemLine = ({ title }) => (
    <div className="poem-header">
        <h1 className="poem-header__title"> {title}</h1>
    </div>
)

export default PoemLine
